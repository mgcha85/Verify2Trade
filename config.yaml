# System Configuration

server:
  host: "0.0.0.0"
  port: 3000

backtest:
  data_path: "./cryptodata"
  default_symbol: "BTCUSDT"
  initial_capital: 10000.0

ai:
  local_vlm_model: "qwen3-vl:latest"
  remote_llm_base_url: "https://api.openai.com/v1"
  remote_llm_model: "gpt-4-turbo"
  prompts:
    chart_analysis: |
      Analyze this chart image. Focus on the relationship between price action and the 25 MA line.
      Identify if there was a rejection at the MA line.
